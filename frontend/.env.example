# Resend (Email Service)
RESEND_API_KEY=re_xxxxxxxxxxxxx

# Email Configuration
EMAIL_FROM=onboarding@resend.dev
CONTACT_EMAIL=your-email@example.com


# Public Account Auth (production public accounts; separate from Admin auth)
# Enable the public-account UI surface. Keep disabled until acceptance is
# complete; production must enable backend + frontend flags together.
NEXT_PUBLIC_PUBLIC_ACCOUNT_AUTH_ENABLED="false"
# Backend API base URL used by the public account client.
# Calendar uses this same API base URL; there is no mock/source selector.
NEXT_PUBLIC_API_URL="http://localhost:8080"
# Comma-separated managed media origins allowed by the image cropper.
# Production requires explicit HTTPS origins, for example:
# NEXT_PUBLIC_MEDIA_ALLOWED_ORIGINS="https://media.example.com"
NEXT_PUBLIC_MEDIA_ALLOWED_ORIGINS="http://localhost:9000"
